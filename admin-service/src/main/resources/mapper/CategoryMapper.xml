<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.admin.dao.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.ecommerce.admin.entity.Category">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="parent_id" property="parentId" jdbcType="BIGINT"/>
        <result column="level" property="level" jdbcType="INTEGER"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="VARCHAR"/>
        <result column="update_by" property="updateBy" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, parent_id, level, sort, status, create_time, update_time, create_by, update_by
    </sql>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM category
        ORDER BY sort ASC, create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM category
    </select>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM category
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM category
        ORDER BY sort ASC, create_time DESC
    </select>

    <insert id="insert" parameterType="com.ecommerce.admin.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (
            name, parent_id, level, sort, status, create_time, update_time, create_by, update_by
        ) VALUES (
            #{name}, #{parentId}, #{level}, #{sort}, #{status}, #{createTime}, #{updateTime}, #{createBy}, #{updateBy}
        )
    </insert>

    <update id="update" parameterType="com.ecommerce.admin.entity.Category">
        UPDATE category
        SET name = #{name},
            parent_id = #{parentId},
            level = #{level},
            sort = #{sort},
            status = #{status},
            update_time = #{updateTime},
            update_by = #{updateBy}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper> 