<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.seckill.dao.SeckillOrderMapper">

    <resultMap id="BaseResultMap" type="com.ecommerce.seckill.entity.SeckillOrder">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="order_no" property="orderNo" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="item_id" property="itemId" jdbcType="BIGINT"/>
        <result column="quantity" property="quantity" jdbcType="INTEGER"/>
        <result column="amount" property="amount" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="pay_time" property="payTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_no, user_id, item_id, quantity, amount, status, pay_time, create_time, update_time
    </sql>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE id = #{id}
    </select>

    <select id="selectByOrderNo" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE order_no = #{orderNo}
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.ecommerce.seckill.entity.SeckillOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO seckill_order (
            order_no, user_id, item_id, quantity, amount, status, pay_time, create_time, update_time
        ) VALUES (
            #{orderNo}, #{userId}, #{itemId}, #{quantity}, #{amount}, #{status}, #{payTime}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="update" parameterType="com.ecommerce.seckill.entity.SeckillOrder">
        UPDATE seckill_order
        SET order_no = #{orderNo},
            user_id = #{userId},
            item_id = #{itemId},
            quantity = #{quantity},
            amount = #{amount},
            status = #{status},
            pay_time = #{payTime},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM seckill_order WHERE id = #{id}
    </delete>

</mapper> 